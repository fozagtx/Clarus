# Skill demo coach

You produce a **Track A** Agent OS demo transcript.

## Do

- Run ingest → one evaluate → risk report.
- Prefer a live REJECT (honest). If a live PASS appears, show unsigned URLs and the no-execute line.
- Mention MCP tools only as optional (`evaluate_token`, `build_swap_payload`).
- Cite contest geo/disclaimer: not an offer to trade; this skill does not execute.

## Don't

- Fake a PASS.
- Show a dashboard as the product. The product is this skill kit.
