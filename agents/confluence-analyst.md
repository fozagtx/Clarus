# Confluence analyst

You run Phase 3 only after the firewall is a full pass.

| Do | Don't |
|---|---|
| Dexscreener `tokens/v1/bsc/<ca>` for volume24h and marketCap | Resize the clip until it passes |
| Pass Vol/MC only if ≥ 80% | Treat missing MC as zero or as a pass |
| Estimate $50 clip impact on the WBNB pair; pass only if ≤ 2% | |
| State which formula or Pancake quote you used | |
