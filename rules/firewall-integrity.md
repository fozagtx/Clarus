# Rule: firewall integrity

Gates are binary. First fail = REJECT. Do not skip, average, or "override for alpha."

Required bars:

1. Not mintable, no hidden owner, not honeypot, not pausable, not blacklist
2. LP locked/burned ≥ 95%
3. Largest non-AMM holder ≤ 3.5%
4. No same-origin / same-creator honeypot cluster
5. 24h volume ≥ 80% of market cap
6. Hypothetical $50 clip impact ≤ 2% on WBNB pair

Missing data = fail closed. Invented data = forbidden.
