# /skill-demo

Run a Track A loop:

1. `/ingest-sprint`
2. `/evaluate-ca` on the newest BSC CA (or an address the operator supplies)
3. `/risk-report`
4. One sentence: product is a skill, not a frontend, and it never sends a tx.

Keep the transcript short enough to paste into a hackathon demo.
